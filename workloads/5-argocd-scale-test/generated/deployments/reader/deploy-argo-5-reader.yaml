apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-argo-5-reader
  labels:
    appname: argocd-scale-dr
spec:
  replicas: 1
  selector:
    matchLabels:
      app-key: argo-5
  template:
    metadata:
      labels:
        app-key: argo-5
        appname: argocd-scale-dr
        scale-test: spread
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              scale-test: spread
      containers:
        - name: busybox
          image: quay.io/nirsof/busybox:stable
          command: ["tail", "-f", "/dev/null"]
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pvc-argo-5
